name: example
description: A complete working example of the vm_result package showing state management and effects.
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  vm_result:
    path: ../
